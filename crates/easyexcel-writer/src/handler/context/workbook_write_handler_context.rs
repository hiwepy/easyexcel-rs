//! Java package-path alias for the real workbook handler context.

/// The Java-compatible name resolves to the runtime callback type.
pub type WorkbookWriteHandlerContext = easyexcel_core::WriteWorkbookContext;
